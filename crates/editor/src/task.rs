//! Stub re-export of the removed `task` crate for editor submodules.
pub use crate::stubs::{
    DebugScenario, ResolvedTask, RevealStrategy, RunnableTag, TaskContext, TaskSourceKind,
    TaskTemplate, TaskVariables, VariableName,
};
